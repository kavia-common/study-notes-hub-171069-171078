import React from 'react';

/**
 * PUBLIC_INTERFACE
 * Upload page: will host a form to upload new notes (PDFs) with metadata.
 */
export default function Upload() {
  return (
    <div className="card">
      <h1>Upload Notes</h1>
      <p className="text-muted">Sign in to upload your PDF notes and share with others.</p>
    </div>
  );
}
